<%= form_with(model: @title, local:true) do |f| %>
  <%= render 'layouts/error_messages', model: f.object %>
		<%= f.text_field :title %>
	<%= f.submit 'Post', class: 'btn btn-sm', id: 'titlebtn' %>
<% end %>						